# RE — Session 2026-07-05

## Contexto
Retomé el pendiente del 4-jul: el prompt "next level" de InmoFindr que había quedado
diseñado pero nunca ejecutado (repo sin commits desde 09-may). Nico eligió el **track
agresivo (VPS) acá**, con OK puntual antes de cada paso de prod. ML **diferido** (decisión
de Nico).

## Hallazgo mayor: el nightly estaba muerto hace ~2 meses
El log del nightly no se escribía desde el **2026-05-09 06:17**. Cron activo (disparaba
otros jobs), pero el comando abortaba antes de entrar al script. Causa raíz confirmada:
`export $(cat .env | xargs)` (en el crontab **y** en `nightly-update.sh`) salía con exit 1
porque `.env` sumó una línea de comentario el 9-may (al cablear el proxy). El `#` expandido
por xargs no es comentario de shell → palabras como "Re-enable" llegan a `export` como
identificadores inválidos → el `&&` corta → el pipeline nunca corría. Esto congeló todo el
dataset (turnover MV en 04-may, descripciones estancadas, active cayendo por stale).

## Entregado
1. **Radar de Oportunidades** (`/oportunidades`): vista `opportunity_scan` (sql/) sobre
   `price_per_covered_sqm` (evita distorsión de lote) vs mediana del barrio, con
   `opportunity_score` (gap capado a 50% × confianza de área × liquidez del barrio) y
   contexto de `turnover_metrics_weekly`. Página con KPIs + scatter precio/descuento +
   tabla rankeada/filtrable. 502 oportunidades bajo-mediana (407 verified). Verificado
   end-to-end en browser headless. nav.js actualizado.
2. **Fix paginación ZP (task #7)** en `scan-zp-headless.mjs`: paginar por click in-site
   (`PAGING_NEXT`) en vez de `goto` frío a `-pagina-N.html` (lo que disparaba el CF
   challenge) + `domcontentloaded` en vez de `networkidle2` (ZP nunca "idle" por ads) +
   rotación IP/re-warm como fallback + budget contado. Validado en prod: page 1 (26) +
   page 2 (30) en 62s. Era el bloqueante desde mayo.
3. **Nightly resucitado**: `nightly-update.sh` carga `.env` con `set -a; . ./.env; set +a`;
   crontab del VPS sin el `export xargs` frágil; backups `.env.bak-20260705` +
   `/root/crontab.bak-20260705`. Caps env-overridables sized para 1GB (ZP_PAGES=6,
   AP_PAGES=6, ENRICH_MAX_BATCHES=3) — el fix de paginación hizo que el scan sea 3× más
   profundo, así que sin re-tuneo el nightly corría >1h con riesgo de OOM.
4. **Semantic v2**: RPC `semantic_search_hybrid` (vector UNION full-text OR-ificado sobre
   el `fts` tsvector) + edge fn `semantic-search` v5. Cubre el ~93% de props sin embedding
   (embeddings starved: solo ~7% activas por falta de descripciones → depende del enrich).
   Verificado live (HTTP 200, vector-first + text fallback).
5. **QA /advanced** (#1): 0 errores de consola, barra de filtros global YA existía y anda,
   6 charts renderizan. **Doc fix** (#2): CLAUDE.md issues actualizado (ZP resuelto, outage
   documentado, "D3 CSV" era stale, nota de embeddings).

## Estado data
- Backlog de enrich ZP: ~1245 activas sin enriquecer (solo 72 con descripción). Se limpia
  en ~4-5 noches con MAX_BATCHES=3, o antes con resize a 2GB (no hecho, guardrail).
- El scan parcial de esta sesión ya subió active de ~2654 → ~3157.

## Commits
48ff169 (radar+ZP) · 16ee8bc (nightly env-load) · 83b6422 (domcontentloaded) ·
156ada4 (nightly caps) · 523c337 (hybrid search + doc).

## Resultado corrida nightly (validación bounded, 17:21, 1556s)
Pipeline complete end-to-end, los 8 pasos. Resurrección **confirmada**.
- ZP scan: casa 101 + ph 113 + depto 100 = **628 nuevas** (venía de 0 hace 2 meses).
- ⚠️ **ZP enrich sigue roto**: "[CB] Fail: empty page" en `/propiedades/*`. Es el CF de
  las **fichas** (issue #6), distinto del scan (#7 que sí arreglé). El fix de paginación
  no ayuda al enrich (fichas son URLs directas, sin path de click in-site). Descripciones
  NO fluyen hasta resolver esto → es el próximo bloqueante para semantic/opportunity.
- ℹ️ Active 3157 → **619** (stale removed 2777). CORRECTO: los 2777 tenían last_seen 2+
  meses (verificado: stale_but_recent=0). El 3157 estaba inflado con fantasmas que la
  stale-detection muerta acumuló. Crece de vuelta al re-confirmar inventario vivo con los
  scans full (6 páginas) de las próximas noches.

## Pendientes
- Confirmar la corrida completa del nightly (validación bounded en curso + cron 06:00 UTC).
- ML: diferido hasta que el nightly resucitado corra estable unas noches (riesgo re-ban).
- Considerar resize VPS a 2GB si el backlog de enrich debe limpiarse más rápido.
- Re-embeddings: a medida que el enrich sume descripciones, correr generate-embeddings.mjs
  para subir cobertura de semantic (hoy ~7%).
